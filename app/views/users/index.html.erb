<%= render_grid(
  @users,
  {
    name: { label: 'Nome' },
    email: { label: 'E-mail' },
    created_at: { 
      label: 'Criado em',
      format: ->(date) { l(date, format: :short) }
    }
  },
  {
    title: 'Usuários',
    new_button: true,
    edit_button: true,
    delete_button: true,
    search: true,
    per_page: 10
  }
) %> 